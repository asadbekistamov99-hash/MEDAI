{
  "name": "medai-functions",
  "description": "Server-side admin verification for MedAI (sets the admin custom claim; the client and firestore.rules only trust this claim, never a client-supplied value)",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^5.1.1"
  },
  "private": true
}
